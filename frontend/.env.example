# The backend API for chat endpoint.
NEXT_PUBLIC_CHAT_API=http://localhost:8000/api/chat

# API key for authenticating requests to the backend (X-API-Key header).
# Must match one of the keys in the backend API_KEYS env var.
NEXT_PUBLIC_API_KEY=your_secure_api_key_here
